<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alpha.reader_materialdesign">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/main_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FileOpenActivity"
            android:label="扫描本地书籍" />
        <activity
            android:name=".BookReadActivity"
            android:launchMode="standard" />
        <activity android:name=".CommunityActivity" />
        <activity
            android:name=".FindBookActivity"
            android:label="新书速递"
            android:launchMode="standard" />
        <activity android:name=".FindBookDetailActivity" />

        <service
            android:name=".Wifi.WebService"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".FindBookActivityB"
            android:label="最受关注"
            android:launchMode="standard" />
        <activity
            android:name=".FindBookActivityC"
            android:label="Top250"
            android:launchMode="standard" />
        <activity android:name=".AccountActivity" />
        <activity android:name=".HadLoginActivity" />
        <activity android:name=".RegisterActivity"></activity>
    </application>

</manifest>